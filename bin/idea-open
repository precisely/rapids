#!/bin/sh
FILENAME=$1
LINENUM=${2:-1}
# COLNUM=${3:-0} # doesn't work on mac
if [ -n "$1" && -n "$2" && -n "$3" ]; then
  echo "This is used by Figwheel to open files in Intellij IDEA"
  echo "usage: idea-open FILENAME LINENUM COLNUM"
else
  idea --line $LINENUM $FILENAME
fi